{% if is_timestamped %}    url(
        regex=r'^{{ model_slug }}/archive/(?P<year>\d{4})/'
               '(?P<month>\d{1,2})/'
               '(?P<day>\d{1,2})/$',
        view={{ class_name }}DayArchiveView.as_view(),
        name='{{ app_name }}_{{ model_slug }}_day_archive'
    ),{% endif %}
